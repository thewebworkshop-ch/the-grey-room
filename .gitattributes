# Normalize line endings to LF for all text files
* text=auto eol=lf

# Lockfile (prevents merge conflicts)
pnpm-lock.yaml text eol=lf

# Binary files (prevents corruption)
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.webp  binary
*.ico   binary
*.pdf   binary
*.woff  binary
*.woff2 binary
*.ttf   binary
*.eot   binary
